# Graphlux
An automated batch processing and compression tool for images and videos, utilizing a Directed Acyclic Graph (DAG) node pipeline architecture. It provides a web interface for local or remote access. Feature highlight: Supports using file metadata to track state and prevent redundant execution, rather than depending solely on a database.
